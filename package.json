{
  "name": "a-game",
  "version": "1.0.0",
  "description": "A game",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.js --bundle > build.js"
  },
  "author": "Rayana e Pablo",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.14.43",
    "kaboom": "^2000.2.9"
  }
}
